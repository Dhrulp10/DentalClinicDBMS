name: Build PyInstaller App (Windows + macOS)

on:
  push:
    branches: [ none ] # Replace 'none' with 'main' to enable on pushes to main branch
  pull_request:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, macos-latest]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install pyinstaller

    # Adjust SQL filenames if needed
    - name: Build executable with PyInstaller
      run: |
        pyinstaller --onefile \
          --add-data "create_tables.sql:." \
          --add-data "drop_tables.sql:." \
          --add-data "populate_tables.sql:." \
          main.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: DentalClinicDBMS-${{ matrix.os }}
        path: dist/
